# Switch

switch statement in Go is comparable to the ones in C, C++, Java, JavaScript, and PHP. The difference is that it only runs the equal case so it does not need a break statement.

### The single switch case and how it works

- The default keyword is conditional
- The expression is computed once. etc...
