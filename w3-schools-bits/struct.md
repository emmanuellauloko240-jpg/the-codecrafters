# Go Struct
Struct is a group of different data types in one variable.

While

array is the process of storing multiple values of same data type in one variable

# Declaration of a struct in GO:
-To declare a struct in Go, used the type and struct keywords.

# Access struct Members
To access member of a structure, use the dot operator (.) between the structure variable name and the structure member

