# maps

Maps in Go are used to store data in key:value pairs Each item in a map has a key and a value Maps are unordered meaning the order of elements isn’t certained and they are changeable, so you can add, or remove items Maps cannot have duplicate keys, and their length can be found using the len() function By standard a map is nil until you create it.
