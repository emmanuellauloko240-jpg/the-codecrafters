# Arrays

An array is a single variable that can hold multiple values of the same type. Go arrays start at index 0, meaning [0] is the first item, [1] the second, and so forth.

# Declaration of an array in Go 
- with the var keyword
- with the := sign

